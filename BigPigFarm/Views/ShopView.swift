// ShopView — 4-tab shop interface for purchasing facilities and items.
// Maps from: ui/screens/shop_screen.py
// TODO(0t3): Implement full ShopView content (Facilities, Perks, Upgrades, Adoption tabs)
import SwiftUI

/// The main shop view with tabs for different item categories.
struct ShopView: View {
    let gameState: GameState

    var body: some View {
        Text("Shop — coming soon")
            .navigationTitle("Shop")
    }
}
