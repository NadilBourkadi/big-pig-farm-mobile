/// Smoke test to verify BigPigFarmCore compiles and links correctly via swift test.
import Testing
@testable import BigPigFarmCore

@Test func corePackageCompiles() {
    // If this test runs, the package compiled successfully on macOS.
    #expect(true)
}
